[workspace]
members = [".", "./sertyp_macros"]

[workspace.package]
edition = "2024"
authors = ["Uhrendoktor"]
version = "0.1.3"
license = "MIT"

[package]
name = "sertyp"
edition.workspace = true
authors.workspace = true
version.workspace = true
description = "Serialization and deserialization of most typst types into sertyp CBOR format: This includes markdown, math, etc."
license.workspace = true
repository = "https://github.com/Uhrendoktor/sertyp"
readme = "README.md"
keywords = ["wasm", "serialization", "deserialization", "typst"]
categories = ["encoding", "wasm", "api-bindings"]

[lib]
crate-type = ["lib"]

[features]
content = ["dep:rangemap", "dep:chumsky", "dep:num-traits"]

[dependencies]
sertyp_macros = { path = "./sertyp_macros", version = "0.1.3" }

paste = "1.0.15"
serde = { version = "1.0.228", features = ["derive"] }
serde_cbor = "0.11.2"
rangemap = { version = "1.7.1", optional = true }
chumsky = { version = "0.13.0", optional = true }
num-traits = { version = "0.2.19", optional = true }

[dev-dependencies]
serde_json = "1.0.150"
